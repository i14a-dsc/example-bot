{
  "name": "example-bot",
  "module": "src/index.ts",
  "type": "module",
  "engineStrict": true,
  "devDependencies": {
    "@eslint/compat": "^1.4.1",
    "@types/bun": "^1.3.5",
    "@types/node": "^24.10.4",
    "@typescript-eslint/parser": "^8.50.1",
    "@typescript/native-preview": "^7.0.0-dev.20251223.1",
    "eslint-plugin-unused-imports": "^4.3.0",
    "typescript-eslint": "^8.50.1"
  },
  "peerDependencies": {
    "typescript": "^5.9.3"
  },
  "dependencies": {
    "axios": "^1.13.2",
    "boxen": "^8.0.1",
    "chalk": "^5.6.2",
    "discord-api-types": "^0.38.37",
    "discord.js": "^14.25.1",
    "dotenv": "^17.2.3",
    "eslint": "^9.39.2",
    "fast-glob": "^3.3.3",
    "husky": "^9.1.7"
  },
  "scripts": {
    "start": "bun run src/index.ts",
    "dev": "bun run --hot --watch src/index.ts --development",
    "build": "echo Build is unsupported for this project. Because it uses dynamic imports.",
    "rmcache": "rm -rf node_modules/.cache && echo Cache removed.",
    "lint": "echo Running eslint... && eslint . && echo Running tsc... && tsgo --noEmit && echo Linting completed.",
    "lint:fix": "echo Running eslint --fix... && eslint . --fix &&  echo Linting completed.",
    "watch": "bun run --hot src/index.ts",
    "test": "echo No tests defined.",
    "fmt": "echo Running prettier... && prettier --write . && echo Formatting completed.",
    "env:linux": "export PATH=\"$PATH:./node_modules/.bin\"",
    "env:windows": "set PATH=%PATH%;./node_modules/.bin",
    "update": "git pull"
  }
}
